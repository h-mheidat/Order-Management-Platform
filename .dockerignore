# Keeps the build context small and, more importantly, keeps secrets out of it. Anything sent to the
# daemon can end up in an image layer.
.env
*.pem
*.p12
*.jks

target/
.git/
.gitignore
.idea/
*.iml
.DS_Store
Dockerfile
.dockerignore
docker-compose.yml
docker/
*.md
